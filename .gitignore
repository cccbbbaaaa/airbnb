# Python 虚拟环境 / Python Virtual Environment
venv/
env/
ENV/
.venv/

# Python 缓存文件 / Python Cache Files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# 数据文件（避免推送大型数据文件）/ Data Files (avoid pushing large data files)
data/**/*.csv
data/**/*.xlsx
data/**/*.json
data/**/*.parquet
data/**/*.geojson
# 但保留数据字典等文档 / But keep data dictionary and other documentation
!data/*.md
!data/*.txt
!data/**/*.md
!data/**/*.txt
# 特别忽略合并后的数据文件（通常很大）/ Especially ignore merged data files (usually very large)
data/merged/*.csv
data/merged/*.xlsx

# IDE 配置文件 / IDE Configuration Files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook 检查点 / Jupyter Notebook Checkpoints
.ipynb_checkpoints/

# 输出文件 / Output Files
output/
# charts 目录中的图表和模型文件（可通过运行脚本重新生成）/ Chart and model files in charts directory (can be regenerated by running scripts)
charts/**/*.png
charts/**/*.jpg
charts/**/*.pdf
charts/**/*.svg
charts/**/*.cbm
charts/**/*.csv
# 但保留 charts/charts_for_report 文件夹的图表（用于报告）/ But keep charts in charts/charts_for_report (for reports)
!charts/charts_for_report/**/*.png
!charts/charts_for_report/**/*.jpg
!charts/charts_for_report/**/*.pdf
!charts/charts_for_report/**/*.svg
# 但保留 charts 目录结构和说明文件 / But keep charts directory structure and documentation
!charts/.gitkeep
!charts/**/.gitkeep

# 日志文件 / Log Files
*.log

# 环境变量文件 / Environment Variables
.env
.env.local

# 临时文件 / Temporary Files
*.tmp
*.temp

# CatBoost自动生成的训练日志文件夹 / CatBoost Auto-generated Training Log Folder
catboost_info/